import React from 'react'
import Test from 'react-addons-test-utils'
import chai from 'chai'
const { expect } = chai

describe('test', () => {
    it('works', () => {
        expect(1).to.equal(1)
    })
})
